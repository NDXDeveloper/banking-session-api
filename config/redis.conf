# Configuration Redis pour Banking Session API

# Sécurité
requirepass ${REDIS_PASSWORD}
protected-mode yes
port 6379
bind 0.0.0.0

# Persistance
save 900 1
save 300 10
save 60 10000

# Configuration de la mémoire
maxmemory 256mb
maxmemory-policy allkeys-lru

# Logging
loglevel notice
logfile ""

# Performance
tcp-keepalive 300
timeout 300

# Sécurité additionnelle
rename-command FLUSHDB ""
rename-command FLUSHALL ""
rename-command EVAL ""
rename-command DEBUG ""
rename-command CONFIG banking_config_cmd

# Limites de clients
maxclients 1000

# Configuration des sessions
databases 16
notify-keyspace-events Ex

# AOF (Append Only File)
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec
no-appendfsync-on-rewrite no
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# RDB
stop-writes-on-bgsave-error yes
rdbcompression yes
rdbchecksum yes
dbfilename dump.rdb

# Réplication (si nécessaire)
# replica-serve-stale-data yes
# replica-read-only yes

# Modules de sécurité
# loadmodule /path/to/redisbloom.so

# Configuration avancée pour les sessions bancaires
hash-max-ziplist-entries 512
hash-max-ziplist-value 64
list-max-ziplist-size -2
list-compress-depth 0
set-max-intset-entries 512
zset-max-ziplist-entries 128
zset-max-ziplist-value 64
hll-sparse-max-bytes 3000
stream-node-max-bytes 4096
stream-node-max-entries 100

# Configuration TLS (décommentez si vous utilisez TLS)
# port 0
# tls-port 6380
# tls-cert-file /etc/ssl/certs/redis.crt
# tls-key-file /etc/ssl/private/redis.key
# tls-dh-params-file /etc/ssl/certs/redis.dh
# tls-ca-cert-file /etc/ssl/certs/ca.crt
# tls-protocols "TLSv1.2 TLSv1.3"